{% extends "base.njk" %}
{% block content %}
    <div class="app-layout__content">
        {# TODO we need better copy here #}
        <h1 class="govuk-heading-l">Something went wrong.</h1>
        <p class="govuk-body">{{ message }}</p>
        {% if errorDetails %}
            <pre>Error Details: {{ errorDetails }}</pre>
        {% endif %}
        <a class="govuk-link" href="/">Go back to the home page</a>
    </div>
{% endblock %}
