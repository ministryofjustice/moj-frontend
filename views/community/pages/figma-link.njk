{% from "govuk/components/input/macro.njk" import govukInput %}
{% from "govuk/components/character-count/macro.njk" import govukCharacterCount %}
{% from "govuk/components/button/macro.njk" import govukButton %}
{% from "govuk/components/error-summary/macro.njk" import govukErrorSummary %}
{% from "govuk/components/back-link/macro.njk" import govukBackLink %}
{% extends "base.njk" %}
{% block content %}
  <div class="app-layout__content">
    {% if backLink %}
      {{ govukBackLink({
        text: "Back",
        href: backLink
      }) }}
    {% endif %}
    {% if errorList %}
      {{ govukErrorSummary({
        titleText: "There is a problem",
        errorList: errorList
      }) }}
    {% endif %}
    <form method="POST" action="{{ submitUrl }}" data-add-another-container>
      <span class="govuk-caption-l">Submit a new component</span>
      <h1 class="govuk-heading-xl">Figma design</h1>
      {{ govukInput({
        hint: {
          text: "Set the sharing permissions for the Figma design file so that it can be viewed."
        },
        label: {
          text: "Add a link to the Figma file",
          classes: "govuk-label--m"
        },
        id: "figma-link",
        name: "figmaLink",
        errorMessage: formErrors.figmaLink,
        value: formData.figmaLink,
        autocapitalize: false
      }) }}

      {{ govukCharacterCount({
        name: "figmaLinkAdditionalInformation",
        id: "figmaLink-additional-information",
        maxwords: 250,
        hint: {
          text: "This might include the password for a protected file, or a link expiry date."
        },
        label: {
          text: "Add information about the Figma design file (optional)",
          classes: "govuk-label--m"
        },
        errorMessage: formErrors.figmaLinkAdditionalInformation,
        value: formData.figmaLinkAdditionalInformation
      }) }}

      <input type="hidden" name="_csrf" value="{{ csrfToken }}" />

      <div class="govuk-button-group moj-button-action">
        {{ govukButton({
          text: 'Continue',
          preventDoubleClick: true
        }) }}
        {% if showAddAnother %}

        {{ govukButton({
          text: "Add another link",
          classes: "govuk-button--secondary",
          name: "addAnother"
        }) }}
        {% endif %}
      </div>
    </form>
  </div>
{% endblock %}
