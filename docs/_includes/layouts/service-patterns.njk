{% extends "./base.njk" %}

{% from "macros/heading/macro.njk" import heading %}

{% block content %}
    <div class="app-layout__content">
        {{ heading({
          text: title,
          caption: "Service patterns" if not isIndex
        }) }}

        {{ content | safe }}
    </div>
{% endblock %}
